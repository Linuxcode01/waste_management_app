import 'package:flutter/material.dart';
class Contentpolicy extends StatelessWidget {
  const Contentpolicy({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Content Policy"),
      ),
      body: Center(
        child: Text("Content Policy Screen"),
      ),
    );
  }
}
